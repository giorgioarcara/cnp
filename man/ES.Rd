% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equivalent_scores.R
\name{ES}
\alias{ES}
\title{Calculate Equivalent Scores (ES) cut-offs}
\usage{
ES(n = NULL, adjscores = NULL, lower_tail = TRUE)
}
\arguments{
\item{n}{Numeric. Sample size. Ignored (and re-derived) if \code{adjscores} is
supplied. Must have length 1.}

\item{adjscores}{Optional numeric vector of adjusted scores. If supplied,
cut-off values are returned in addition to observation counts.}

\item{lower_tail}{Logical. If \code{TRUE} (default), lower adjusted scores are
treated as more impaired (ES0 at the bottom of the distribution); if
\code{FALSE}, the ranking is reversed.}
}
\value{
If \code{adjscores} is \code{NULL}, a named numeric vector of observation
counts for the four ES cut-offs. If \code{adjscores} is supplied, a list with
\code{Observations} (the same counts) and \code{Adjusted_Scores} (the
corresponding cut-off values in the input scale).
}
\description{
Calculates the Equivalent Scores classification (ES0/oTL through ES4)
either as observation counts (ranks) for a normative sample of size \code{n},
or directly as adjusted-score cut-off values when \code{adjscores} is supplied.
Adapted from Aiello & Depaoli (2022).
}
