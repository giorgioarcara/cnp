% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regr_methods.R
\name{adjscores_C1987_v1}
\alias{adjscores_C1987_v1}
\title{Adjusted scores via the Capitani (1987) method (version 1)}
\usage{
adjscores_C1987_v1(
  df = NULL,
  dep = "Dep",
  dep.range = c(0, 30),
  age = "Age",
  edu = "Education",
  sex = "Sex",
  ID = "ID"
)
}
\arguments{
\item{df}{A data.frame with the raw data.}

\item{dep}{Character. Name of the dependent variable column.}

\item{dep.range}{Numeric vector of length 2. Range of the dependent
variable's raw scores. Adjusted scores are not extrapolated beyond this
range (ceiling/floor effects are left uncorrected, as in the original
Equivalent Scores method).}

\item{age}{Character. Name of the numeric age column.}

\item{edu}{Character. Name of the numeric education column.}

\item{sex}{Character. Name of the sex column (factor/character with 2
levels, or already-numeric 0/1).}

\item{ID}{Character. Name of an ID column (currently unused; kept for
interface compatibility).}
}
\value{
A list with:
\describe{
\item{new.df}{The input data (NA-omitted) with \code{ADJ_SCORES} and
\code{RESIDUALS} columns added.}
\item{lm.model}{The fitted \code{lm} model.}
\item{transfs}{Character vector with the two selected transformations
for age and education.}
\item{model_text}{A human-readable string describing the fitted
equation.}
}
}
\description{
Calculates adjusted scores via regression modeling, following the method
described in Capitani's chapter in Spinnler & Tognoni (1987). The best
transformation for age and education is first selected independently
(highest univariate R-squared), a multivariate model with Sex is then
fitted, and non-significant terms (Bonferroni-corrected, alpha = 0.05/3)
are dropped \strong{all at once, in a single step}.
}
\details{
Because terms are dropped all together in one step, a term initially kept
(p < 0.017) can end up in a model where it is no longer significant once
the other terms are removed. See \code{\link[=adjscores_C1987_v2]{adjscores_C1987_v2()}} for the iterative
alternative, which re-checks significance after each single-term removal.
}
