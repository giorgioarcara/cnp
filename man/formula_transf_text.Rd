% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{formula_transf_text}
\alias{formula_transf_text}
\title{Build a model formula (as text) applying named transformations}
\usage{
formula_transf_text(
  transfs = NULL,
  pred.names = NULL,
  dep.name = NULL,
  data.name = NULL
)
}
\arguments{
\item{transfs}{Character vector of transformation names (e.g.
\code{c("quadr", "cube")}; \code{""} means no transformation is applied).}

\item{pred.names}{Character vector of predictor names (e.g.
\code{c("Age", "Edu")}), same length as \code{transfs}.}

\item{dep.name}{Character. Name of the dependent variable.}

\item{data.name}{Character. Name of the data.frame (as it appears in the
calling environment) to pass to \verb{data = }.}
}
\value{
A character string with the full \code{lm(...)} call text, suitable for
\code{eval(parse(text = ...))}.
}
\description{
Internal function to create the text of a \code{lm()} call applying the
identified transformation to each predictor. Used by the \verb{adjscores_*()}
functions to build their final model formula.
}
\keyword{internal}
