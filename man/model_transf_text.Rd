% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{model_transf_text}
\alias{model_transf_text}
\title{Turn a model's coefficients into a readable equation string, substituting transformation names}
\usage{
model_transf_text(
  mod,
  transfs = NULL,
  transfs.names = NULL,
  new.names = NULL,
  digits = 3
)
}
\arguments{
\item{mod}{A fitted model (e.g. \code{lm.model} from an \verb{adjscores_*()} call).}

\item{transfs}{Character vector of transformation names used in the model
(to be substituted with \code{transfs.names}).}

\item{transfs.names}{Character vector of the internal variable names used
when fitting the model (e.g. \code{c("age_tr", "edu_tr")}).}

\item{new.names}{Character vector of the readable labels to use in the
output formula (e.g. \code{c("Age", "Edu")}).}

\item{digits}{Integer. Number of significant digits to keep for each
coefficient.}
}
\value{
A character string with the readable equation.
}
\description{
Internal function to transform a model into an equation string (e.g.
\code{"12 + 0.8 * log(Age) + 0.002 * quadr(Edu) + 0.2 * Sex"}), substituting
the internal transformed-variable names (e.g. \code{age_tr}) used when fitting
the model with a readable \code{transformation(OriginalName)} form. Used by
\code{\link[=adjscores_A2024_v1]{adjscores_A2024_v1()}} and the other \verb{adjscores_*()} functions.
}
\keyword{internal}
