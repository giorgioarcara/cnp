% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{sample.coef.unif}
\alias{sample.coef.unif}
\title{Sample regression coefficients from a uniform grid}
\usage{
sample.coef.unif(
  c.range = list(c(10, 12), c(-0.05, 0.1), c(0.04, 0.08), c(-0.01, 0.01)),
  c.steps = c(100, 100, 100, 100)
)
}
\arguments{
\item{c.range}{A list of two-element numeric vectors, each giving the
minimum and maximum value a coefficient can take.}

\item{c.steps}{Numeric vector, same length as \code{c.range}. Number of
equally-spaced candidate values generated within each range before one
is sampled (e.g. \code{c.steps[1] = 100} means 100 candidate values are
generated within \code{c.range[[1]]} and one is drawn from them).}
}
\value{
A list of sampled coefficient values, one per element of
\code{c.range}.
}
\description{
Generates values from a uniform (equiprobable) distribution within given
ranges, one value per element of \code{c.range}. Used to generate random
regression coefficients (e.g. by \code{\link[=sim.norm.data]{sim.norm.data()}}).
}
