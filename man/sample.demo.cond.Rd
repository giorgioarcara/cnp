% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{sample.demo.cond}
\alias{sample.demo.cond}
\title{Sample demographic values with age-conditioned probabilities}
\usage{
sample.demo.cond(
  n,
  age_values_t = seq(18, 90, 1),
  edu_values_t = c(5, 8, 13, 18, 20),
  sex_values_t = c(0, 1),
  cond.age.edu = list(list(c(18, 22), c(23, 40), c(41, 50), c(51, 60), c(61, 70), c(71,
    80), c(81, 90)), list(c(0, 0, 1, 0, 0), c(0, 0, 0.5, 0.3, 0.2), c(0, 0, 0.55, 0.3,
    0.15), c(0, 0.2, 0.6, 0.1, 0.1), c(0, 0.3, 0.5, 0.1, 0.1), c(0.4, 0.3, 0.2, 0.05,
    0.05), c(0.3, 0.5, 0.15, 0.025, 0.025))),
  cond.age.sex = list(list(c(18, 90)), list(c(0.5, 0.5))),
  details = F
)
}
\arguments{
\item{n}{Integer. Number of participants to generate.}

\item{age_values_t}{Numeric vector of candidate age values.}

\item{edu_values_t}{Numeric vector of candidate education values.}

\item{sex_values_t}{Numeric vector of candidate sex values (0/1).}

\item{cond.age.edu}{A list of two elements of equal length: (1) a list of
two-element age-range vectors (e.g. \code{list(c(18,22), c(23,40), ...)}),
and (2) a list of probability vectors (same length as \code{edu_values_t},
each summing to 1) giving the education probabilities for participants
in the corresponding age range.}

\item{cond.age.sex}{Same structure as \code{cond.age.edu}, but for sex
probabilities conditioned on age range.}

\item{details}{Logical. If \code{TRUE}, prints an additional diagnostic table
of the age/education conditioning groups actually used.}
}
\value{
A data.frame with columns \code{age_values_o}, \code{edu_values_o},
\code{sex_values_o}.
}
\description{
Generates age values uniformly, then generates education and sex values
for each simulated participant using age-conditioned probability
distributions, so that (e.g.) younger simulated participants can be given
a different education/sex distribution than older ones.
}
