% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{sim.norm.data}
\alias{sim.norm.data}
\title{Simulate normative data with a known generative model}
\usage{
sim.norm.data(
  n,
  coefs = sample.coef.unif(c.range = list(c(18, 20), c(0, -4), c(0, 4), c(-1, 1)),
    c.steps = c(100, 100, 100, 100)),
  eff_mult = NULL,
  P_score.sd = 1,
  age_values_o = 50,
  edu_values_o = 15,
  sex_values_o = "F",
  age_transf = NULL,
  edu_transf = NULL,
  details = FALSE
)
}
\arguments{
\item{n}{Integer. Number of participants to generate.}

\item{coefs}{A list of 4 values (Intercept, Age, Education, Sex
coefficients) determining the generative model's coefficients. The
final effect on the observed score also depends on \code{age_transf}/
\code{edu_transf}. Defaults to a call to \code{\link[=sample.coef.unif]{sample.coef.unif()}}.}

\item{eff_mult}{Deprecated, unused.}

\item{P_score.sd}{Numeric. Standard deviation of the noise added to the
demographic-predicted score to obtain each participant's "true"/observed
score (i.e. the residual from the demographic regression model).}

\item{age_values_o}{Numeric vector (or scalar) of observed age values.}

\item{edu_values_o}{Numeric vector (or scalar) of observed education
values.}

\item{sex_values_o}{Vector (or scalar) of observed sex values: either
\code{"M"}/\code{"F"} character values, or numeric 0/1 values (e.g. as produced by
\code{\link[=sample.demo.unif]{sample.demo.unif()}} / \code{\link[=sample.demo.cond]{sample.demo.cond()}}).}

\item{age_transf}{Character. Name of the transformation determining the
relationship between age and the score (can be randomly generated with
\code{\link[=sample.transf]{sample.transf()}}).}

\item{edu_transf}{Character. Name of the transformation determining the
relationship between education and the score (can be randomly generated
with \code{\link[=sample.transf]{sample.transf()}}).}

\item{details}{Logical. If \code{TRUE}, the returned data.frame also includes
the separate age/education/sex contributions to the final score.}
}
\value{
A list with: a data.frame of simulated normative data (age,
education, sex, demographic score, participant noise, observed score,
...), a human-readable string describing the generative model, the
coefficients used, and the fitted \code{lm.model} used to derive the readable
formula.
}
\description{
Simulates normative data for \code{n} participants from a linear generative
model in age, education, and sex, plus normally-distributed noise. With
the default settings, a single female participant with age 50 and
education 15 is generated. This is the core function used to generate
ground-truth data for testing/comparing normative adjustment methods
(see \code{\link[=adjscores_A2024_v1]{adjscores_A2024_v1()}}, \code{\link[=adjscores_C1987_v1]{adjscores_C1987_v1()}}, etc.).
}
